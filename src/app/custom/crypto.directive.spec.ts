import { CryptoDirective } from './crypto.directive';

describe('CryptoDirective', () => {
  it('should create an instance', () => {
    const directive = new CryptoDirective();
    expect(directive).toBeTruthy();
  });
});
